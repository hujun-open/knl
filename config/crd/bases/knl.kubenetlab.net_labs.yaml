---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: labs.knl.kubenetlab.net
spec:
  group: knl.kubenetlab.net
  names:
    kind: Lab
    listKind: LabList
    plural: labs
    singular: lab
  scope: Namespaced
  versions:
  - name: v1beta1
    schema:
      openAPIV3Schema:
        description: Lab is the Schema for the labs API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of Lab
            properties:
              links:
                additionalProperties:
                  properties:
                    gwAddr:
                      type: string
                    nodes:
                      items:
                        properties:
                          addr:
                            type: string
                          mac:
                            type: string
                          node:
                            type: string
                          port:
                            type: string
                        required:
                        - node
                        type: object
                      type: array
                  required:
                  - nodes
                  type: object
                nullable: true
                type: object
              nodes:
                additionalProperties:
                  description: OneOfSystem specifies one KNL node type, only one field
                    should be specified.
                  properties:
                    magc:
                      description: MAGC specifies a Nokia MAG-c
                      nullable: true
                      properties:
                        chassis:
                          description: specifies chassis configuration
                          nullable: true
                          properties:
                            cards:
                              additionalProperties:
                                description: SRCard is a CPM or IOM card
                                properties:
                                  cpu:
                                    anyOf:
                                    - type: integer
                                    - type: string
                                    description: requested CPU in k8s resource unit
                                    nullable: true
                                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                    x-kubernetes-int-or-string: true
                                  mdas:
                                    description: list of MDAs that are insert directly
                                      into card without XIOM; mdas and xioms are mutully
                                      exclusive
                                    items:
                                      type: string
                                    type: array
                                  memory:
                                    anyOf:
                                    - type: integer
                                    - type: string
                                    description: requested memory in k8s resouce unit
                                    nullable: true
                                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                    x-kubernetes-int-or-string: true
                                  ports:
                                    description: list of listening ports for management
                                      interface
                                    items:
                                      description: |-
                                        Port represents a port to expose from the virtual machine.
                                        Default protocol TCP.
                                        The port field is mandatory
                                      properties:
                                        name:
                                          description: |-
                                            If specified, this must be an IANA_SVC_NAME and unique within the pod. Each
                                            named port in a pod must have a unique name. Name for the port that can be
                                            referred to by services.
                                          type: string
                                        port:
                                          description: |-
                                            Number of port to expose for the virtual machine.
                                            This must be a valid port number, 0 < x < 65536.
                                          format: int32
                                          type: integer
                                        protocol:
                                          description: |-
                                            Protocol for port. Must be UDP or TCP.
                                            Defaults to "TCP".
                                          type: string
                                      required:
                                      - port
                                      type: object
                                    nullable: true
                                    type: array
                                  sysinfo:
                                    description: sysinfo is only used by vsim, mag-c
                                      and vsri
                                    type: string
                                  type:
                                    description: Card model
                                    type: string
                                  xioms:
                                    additionalProperties:
                                      description: SR XIOM
                                      properties:
                                        mdas:
                                          description: list of MDAs insert into the
                                            XIOM
                                          items:
                                            type: string
                                          type: array
                                        type:
                                          description: XIOM model
                                          type: string
                                      type: object
                                    description: list of XIOMs; key is XIOM slot id,
                                      e.g. x1/x2; mdas and xioms are mutully exclusive
                                    type: object
                                type: object
                              description: |-
                                a dictionary of CPM and IOM cards,
                                key is slot id, "A","B" for CPM, number for IOM
                              type: object
                            model:
                              description: chassis model
                              type: string
                            sfm:
                              description: SFM model
                              type: string
                            type:
                              description: type of chassis, srsim, vsim vsris or magc
                              type: string
                          type: object
                        diskSize:
                          anyOf:
                          - type: integer
                          - type: string
                          description: Disk size for the CPM, only used when image
                            is a docker image, must >= image size
                          nullable: true
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        image:
                          description: |-
                            one of three types of image loading method:
                            1.docker image url like "exampleregistry/sros:25.10.1";
                            2.sub folder name of the SROS/MAGC image when start with "filesvr:", like "filesvr:25.10.1"
                          nullable: true
                          type: string
                        license:
                          description: a k8s secret name contains license with key
                            "license"
                          nullable: true
                          type: string
                        uuid:
                          description: VM's firmware UUID
                          nullable: true
                          type: string
                      type: object
                    pod:
                      description: GeneralPod specifies a general k8s pod
                      nullable: true
                      properties:
                        cmd:
                          description: pod's command
                          type: string
                        cpu:
                          anyOf:
                          - type: integer
                          - type: string
                          description: requested cpu in k8s resource unit
                          nullable: true
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        image:
                          description: pod image
                          type: string
                        memory:
                          anyOf:
                          - type: integer
                          - type: string
                          description: requested memory in k8s resource unit
                          nullable: true
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        privileged:
                          description: privileged pod if true
                          type: boolean
                      type: object
                    srl:
                      description: SRLinux specifies a Nokia SRLinux chassis;
                      nullable: true
                      properties:
                        chassis:
                          description: chassis model
                          type: string
                        cpu:
                          anyOf:
                          - type: integer
                          - type: string
                          description: requested cpu in k8s resource unit
                          nullable: true
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        image:
                          description: SRLinux container image
                          type: string
                        license:
                          description: a k8s secret contains the license file with
                            "license" as the key
                          type: string
                        memory:
                          anyOf:
                          - type: integer
                          - type: string
                          description: requested memory in k8s resource unit
                          nullable: true
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                      type: object
                    srsim:
                      description: |-
                        SRSIM creates a Nokia SR-SIM;
                        note: it is important to set `tx-checksum-ip-generic` off in corresponding bridge interface, otherwise IP traffic toward management interface won't work
                        in kind, it is docker bridge;
                        in general k8s, it is cni0 bridge in each worker;
                        "ethtool -K <interface> tx-checksum-ip-generic off"
                        see SR-SIM installation guide for details
                      nullable: true
                      properties:
                        chassis:
                          description: specifies the chassis configuration
                          nullable: true
                          properties:
                            cards:
                              additionalProperties:
                                description: SRCard is a CPM or IOM card
                                properties:
                                  cpu:
                                    anyOf:
                                    - type: integer
                                    - type: string
                                    description: requested CPU in k8s resource unit
                                    nullable: true
                                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                    x-kubernetes-int-or-string: true
                                  mdas:
                                    description: list of MDAs that are insert directly
                                      into card without XIOM; mdas and xioms are mutully
                                      exclusive
                                    items:
                                      type: string
                                    type: array
                                  memory:
                                    anyOf:
                                    - type: integer
                                    - type: string
                                    description: requested memory in k8s resouce unit
                                    nullable: true
                                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                    x-kubernetes-int-or-string: true
                                  ports:
                                    description: list of listening ports for management
                                      interface
                                    items:
                                      description: |-
                                        Port represents a port to expose from the virtual machine.
                                        Default protocol TCP.
                                        The port field is mandatory
                                      properties:
                                        name:
                                          description: |-
                                            If specified, this must be an IANA_SVC_NAME and unique within the pod. Each
                                            named port in a pod must have a unique name. Name for the port that can be
                                            referred to by services.
                                          type: string
                                        port:
                                          description: |-
                                            Number of port to expose for the virtual machine.
                                            This must be a valid port number, 0 < x < 65536.
                                          format: int32
                                          type: integer
                                        protocol:
                                          description: |-
                                            Protocol for port. Must be UDP or TCP.
                                            Defaults to "TCP".
                                          type: string
                                      required:
                                      - port
                                      type: object
                                    nullable: true
                                    type: array
                                  sysinfo:
                                    description: sysinfo is only used by vsim, mag-c
                                      and vsri
                                    type: string
                                  type:
                                    description: Card model
                                    type: string
                                  xioms:
                                    additionalProperties:
                                      description: SR XIOM
                                      properties:
                                        mdas:
                                          description: list of MDAs insert into the
                                            XIOM
                                          items:
                                            type: string
                                          type: array
                                        type:
                                          description: XIOM model
                                          type: string
                                      type: object
                                    description: list of XIOMs; key is XIOM slot id,
                                      e.g. x1/x2; mdas and xioms are mutully exclusive
                                    type: object
                                type: object
                              description: |-
                                a dictionary of CPM and IOM cards,
                                key is slot id, "A","B" for CPM, number for IOM
                              type: object
                            model:
                              description: chassis model
                              type: string
                            sfm:
                              description: SFM model
                              type: string
                            type:
                              description: type of chassis, srsim, vsim vsris or magc
                              type: string
                          type: object
                        image:
                          description: Docker image
                          nullable: true
                          type: string
                        license:
                          description: name of k8s secret contains license file with
                            "license" as the key
                          nullable: true
                          type: string
                      type: object
                    vm:
                      description: GeneralVM specifies a general kubevirt VM
                      nullable: true
                      properties:
                        cpu:
                          anyOf:
                          - type: integer
                          - type: string
                          description: requested cpu for the VM in k8s resource unit
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        cpuPin:
                          description: pin the CPU if true
                          type: boolean
                        diskSize:
                          anyOf:
                          - type: integer
                          - type: string
                          description: the VM disk size in k8s resource unit
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        hugePage:
                          description: request hugepage memory if true
                          type: boolean
                        image:
                          description: kubevirt CDI supported URL, either HTTP or
                            registry source
                          type: string
                        init:
                          description: intilization method, supports cloud-init or
                            ignition
                          type: string
                        memory:
                          anyOf:
                          - type: integer
                          - type: string
                          description: requested memory for the VM in k8s resource
                            unit
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        passwd:
                          description: password to login into VM
                          type: string
                        ports:
                          description: listening port of the VM
                          items:
                            description: |-
                              Port represents a port to expose from the virtual machine.
                              Default protocol TCP.
                              The port field is mandatory
                            properties:
                              name:
                                description: |-
                                  If specified, this must be an IANA_SVC_NAME and unique within the pod. Each
                                  named port in a pod must have a unique name. Name for the port that can be
                                  referred to by services.
                                type: string
                              port:
                                description: |-
                                  Number of port to expose for the virtual machine.
                                  This must be a valid port number, 0 < x < 65536.
                                format: int32
                                type: integer
                              protocol:
                                description: |-
                                  Protocol for port. Must be UDP or TCP.
                                  Defaults to "TCP".
                                type: string
                            required:
                            - port
                            type: object
                          type: array
                        user:
                          description: username to login into VM, username and password
                            are feed into vm initialization mechinism like cloud-init
                          type: string
                      type: object
                    vsim:
                      description: VSIM specifies a Nokia vSIM router
                      nullable: true
                      properties:
                        chassis:
                          description: specifies chassis configuration
                          nullable: true
                          properties:
                            cards:
                              additionalProperties:
                                description: SRCard is a CPM or IOM card
                                properties:
                                  cpu:
                                    anyOf:
                                    - type: integer
                                    - type: string
                                    description: requested CPU in k8s resource unit
                                    nullable: true
                                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                    x-kubernetes-int-or-string: true
                                  mdas:
                                    description: list of MDAs that are insert directly
                                      into card without XIOM; mdas and xioms are mutully
                                      exclusive
                                    items:
                                      type: string
                                    type: array
                                  memory:
                                    anyOf:
                                    - type: integer
                                    - type: string
                                    description: requested memory in k8s resouce unit
                                    nullable: true
                                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                    x-kubernetes-int-or-string: true
                                  ports:
                                    description: list of listening ports for management
                                      interface
                                    items:
                                      description: |-
                                        Port represents a port to expose from the virtual machine.
                                        Default protocol TCP.
                                        The port field is mandatory
                                      properties:
                                        name:
                                          description: |-
                                            If specified, this must be an IANA_SVC_NAME and unique within the pod. Each
                                            named port in a pod must have a unique name. Name for the port that can be
                                            referred to by services.
                                          type: string
                                        port:
                                          description: |-
                                            Number of port to expose for the virtual machine.
                                            This must be a valid port number, 0 < x < 65536.
                                          format: int32
                                          type: integer
                                        protocol:
                                          description: |-
                                            Protocol for port. Must be UDP or TCP.
                                            Defaults to "TCP".
                                          type: string
                                      required:
                                      - port
                                      type: object
                                    nullable: true
                                    type: array
                                  sysinfo:
                                    description: sysinfo is only used by vsim, mag-c
                                      and vsri
                                    type: string
                                  type:
                                    description: Card model
                                    type: string
                                  xioms:
                                    additionalProperties:
                                      description: SR XIOM
                                      properties:
                                        mdas:
                                          description: list of MDAs insert into the
                                            XIOM
                                          items:
                                            type: string
                                          type: array
                                        type:
                                          description: XIOM model
                                          type: string
                                      type: object
                                    description: list of XIOMs; key is XIOM slot id,
                                      e.g. x1/x2; mdas and xioms are mutully exclusive
                                    type: object
                                type: object
                              description: |-
                                a dictionary of CPM and IOM cards,
                                key is slot id, "A","B" for CPM, number for IOM
                              type: object
                            model:
                              description: chassis model
                              type: string
                            sfm:
                              description: SFM model
                              type: string
                            type:
                              description: type of chassis, srsim, vsim vsris or magc
                              type: string
                          type: object
                        diskSize:
                          anyOf:
                          - type: integer
                          - type: string
                          description: Disk size for the CPM, only used when image
                            is a docker image, must >= image size
                          nullable: true
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        image:
                          description: |-
                            one of three types of image loading method:
                            1.docker image url like "exampleregistry/sros:25.10.1";
                            2.sub folder name of the SROS/MAGC image when start with "filesvr:", like "filesvr:25.10.1"
                          nullable: true
                          type: string
                        license:
                          description: a k8s secret name contains license with key
                            "license"
                          nullable: true
                          type: string
                        uuid:
                          description: VM's firmware UUID
                          nullable: true
                          type: string
                      type: object
                    vsri:
                      description: VSRI specifies a Nokia VSR-I router
                      nullable: true
                      properties:
                        chassis:
                          description: specifies chassis configuration
                          nullable: true
                          properties:
                            cards:
                              additionalProperties:
                                description: SRCard is a CPM or IOM card
                                properties:
                                  cpu:
                                    anyOf:
                                    - type: integer
                                    - type: string
                                    description: requested CPU in k8s resource unit
                                    nullable: true
                                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                    x-kubernetes-int-or-string: true
                                  mdas:
                                    description: list of MDAs that are insert directly
                                      into card without XIOM; mdas and xioms are mutully
                                      exclusive
                                    items:
                                      type: string
                                    type: array
                                  memory:
                                    anyOf:
                                    - type: integer
                                    - type: string
                                    description: requested memory in k8s resouce unit
                                    nullable: true
                                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                    x-kubernetes-int-or-string: true
                                  ports:
                                    description: list of listening ports for management
                                      interface
                                    items:
                                      description: |-
                                        Port represents a port to expose from the virtual machine.
                                        Default protocol TCP.
                                        The port field is mandatory
                                      properties:
                                        name:
                                          description: |-
                                            If specified, this must be an IANA_SVC_NAME and unique within the pod. Each
                                            named port in a pod must have a unique name. Name for the port that can be
                                            referred to by services.
                                          type: string
                                        port:
                                          description: |-
                                            Number of port to expose for the virtual machine.
                                            This must be a valid port number, 0 < x < 65536.
                                          format: int32
                                          type: integer
                                        protocol:
                                          description: |-
                                            Protocol for port. Must be UDP or TCP.
                                            Defaults to "TCP".
                                          type: string
                                      required:
                                      - port
                                      type: object
                                    nullable: true
                                    type: array
                                  sysinfo:
                                    description: sysinfo is only used by vsim, mag-c
                                      and vsri
                                    type: string
                                  type:
                                    description: Card model
                                    type: string
                                  xioms:
                                    additionalProperties:
                                      description: SR XIOM
                                      properties:
                                        mdas:
                                          description: list of MDAs insert into the
                                            XIOM
                                          items:
                                            type: string
                                          type: array
                                        type:
                                          description: XIOM model
                                          type: string
                                      type: object
                                    description: list of XIOMs; key is XIOM slot id,
                                      e.g. x1/x2; mdas and xioms are mutully exclusive
                                    type: object
                                type: object
                              description: |-
                                a dictionary of CPM and IOM cards,
                                key is slot id, "A","B" for CPM, number for IOM
                              type: object
                            model:
                              description: chassis model
                              type: string
                            sfm:
                              description: SFM model
                              type: string
                            type:
                              description: type of chassis, srsim, vsim vsris or magc
                              type: string
                          type: object
                        diskSize:
                          anyOf:
                          - type: integer
                          - type: string
                          description: Disk size for the CPM, only used when image
                            is a docker image, must >= image size
                          nullable: true
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        image:
                          description: |-
                            one of three types of image loading method:
                            1.docker image url like "exampleregistry/sros:25.10.1";
                            2.sub folder name of the SROS/MAGC image when start with "filesvr:", like "filesvr:25.10.1"
                          nullable: true
                          type: string
                        license:
                          description: a k8s secret name contains license with key
                            "license"
                          nullable: true
                          type: string
                        uuid:
                          description: VM's firmware UUID
                          nullable: true
                          type: string
                      type: object
                  type: object
                description: nodes lists all nodes in the lab
                nullable: true
                type: object
            type: object
          status:
            description: status defines the observed state of Lab
            properties:
              conditions:
                description: |-
                  conditions represent the current state of the Lab resource.
                  Each condition has a unique type and reflects the status of a specific aspect of the resource.

                  Standard condition types include:
                  - "Available": the resource is fully functional
                  - "Progressing": the resource is being created or updated
                  - "Degraded": the resource failed to reach or maintain its desired state

                  The status of each condition is one of True, False, or Unknown.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
