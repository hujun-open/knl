apiVersion: v1
kind: Service
metadata:
  labels:
    control-plane: controller-manager
    app.kubernetes.io/name: knl
    app.kubernetes.io/managed-by: kustomize
  name: http
  namespace: system
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: 8880
  selector:
    control-plane: controller-manager
    app.kubernetes.io/name: knl
