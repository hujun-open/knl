apiVersion: v1
kind: Service
metadata:
  labels:
    control-plane: controller-manager
    app.kubernetes.io/name: knl
    app.kubernetes.io/managed-by: kustomize
  name: sftp-service
  namespace: system
spec:
  type: ClusterIP
  ports:
  - name: sftp
    port: 22
    protocol: TCP
    targetPort: 2222
  selector:
    control-plane: controller-manager
    app.kubernetes.io/name: knl
